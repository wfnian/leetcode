#include<stdio.h>
struct xs{
	char car[11];
	char nam[10];
	char sex;
	int age;
	char uni;
	struct xs *next;
};
typedef struct studoc Ifnt;
int main(){
	Ifnt A,B,c,d;
	scanf("%s",&A.car);
//	gets(A.name);
//	A.sex=getchar();
//	fets(B.uni);
	printf("%s\n",A.car);
	
} 
